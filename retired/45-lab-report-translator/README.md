# Lab Report Translator

Turn a manually entered lab marker into plain-language context and a question for a clinician. This app does not diagnose, set treatment targets, or recommend medication.

Typed marker/value inputs and the result persist locally, with validation and reset. It does not upload images or replace a clinician; bring the original report and reference range to a qualified professional.
