# Neighbor Noise Translator

Classify the sound through the wall without escalating the situation.

The app can listen to a two-second microphone sample, measure its level in
memory, and produce a local volume-based translation without saving audio. A
typed description remains the fallback when microphone access is denied or the
runtime is unavailable. It never identifies neighbors or contacts anyone, and
reset clears the wall’s evidence.
